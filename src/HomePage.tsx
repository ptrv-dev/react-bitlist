import { FC } from 'react';
import { Header, HeroSection } from './components';

const HomePage: FC = () => {
  return (
    <>
      <Header />
      <HeroSection />
    </>
  );
};

export default HomePage;
